# NetworkConfiguration

class Conv2D(object):
    def __init__(self, channels, kernel, stride, scope):
        self.channels = channels
        self.kernel = kernel
        self.stride = stride
        self.scope = scope
